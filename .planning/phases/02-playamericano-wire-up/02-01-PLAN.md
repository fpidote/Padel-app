---
phase: 02-playamericano-wire-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/play/PlayAmericano.jsx
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02

must_haves:
  truths:
    - "onNext() reads t.precomputedRounds[t.roundNum] instead of calling buildRoundAmericano() when pre-calculated rounds exist"
    - "onNext() falls back to buildRoundAmericano() without errors when t.precomputedRounds is null or empty (legacy tournament)"
    - "isFinished fires correctly when roundNum exceeds the pre-calculated schedule length, preventing extra rounds"
    - "Admin sees a collapsible warnings banner above the tabs when t.roundWarnings is non-empty"
    - "All players (including spectators) can see the Descansos tab when t.precomputedRounds is populated"
    - "Every round has a row in the Descansos tab showing sitting-out player names or 'Nadie descansa'"
    - "The current round row in Descansos is visually highlighted with yellow styling"
  artifacts:
    - path: "src/components/play/PlayAmericano.jsx"
      provides: "Modified component with onNext() wire-up, isFinished extension, Descansos tab, WarningsBanner"
      contains: "t.precomputedRounds[t.roundNum]"
    - path: "src/components/play/PlayAmericano.jsx"
      provides: "WarningsBanner file-scope sub-component"
      contains: "function WarningsBanner"
  key_links:
    - from: "onNext() line ~114 replacement"
      to: "t.precomputedRounds[t.roundNum]"
      via: "if guard: t.precomputedRounds?.length && t.precomputedRounds[t.roundNum]"
      pattern: "precomputedRounds\\[t\\.roundNum\\]"
    - from: "isAdmin && t.roundWarnings?.length > 0"
      to: "WarningsBanner component"
      via: "JSX conditional above Tabs"
      pattern: "isAdmin && t\\.roundWarnings"
    - from: "Tabs array spread"
      to: "descansos tab entry"
      via: "t.precomputedRounds?.length conditional spread"
      pattern: "descansos.*Descansos"
---

<objective>
Wire PlayAmericano.jsx to consume pre-calculated rounds from Phase 1 and surface two new informational panels to users.

Purpose: Phase 1 built the penalty-scored precomputation engine and stored results in t.precomputedRounds and t.roundWarnings. Phase 2 connects the play interface to actually use those results — advancing rounds from the pre-calculated schedule instead of generating them on-the-fly, showing admins any algorithm relaxation warnings, and giving all participants a rest schedule overview.

Output: A single modified file (src/components/play/PlayAmericano.jsx) with four surgical changes: onNext() wire-up + isFinished extension, conditional Descansos 5th tab with content, and WarningsBanner admin-only sub-component.
</objective>

<execution_context>
@/Users/ximeyfede/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ximeyfede/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-penalty-engine/01-01-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from codebase — read these, do not re-discover them. -->

From src/logic/americano.js (Phase 1 output — DO NOT MODIFY):
  precomputeAllRounds() returns:
    { rounds: Array<{ courts: Array<{ pairA, pairB, scoreA: "", scoreB: "", saved: false }>, sittingOut: Player[] }>, warnings: Array<{ round: number, constraint: string, message: string }> }
  Pairs mode returns: { rounds: null, warnings: [] }

From src/components/shared/Components.jsx lines 27–37:
  export function Tabs({ tabs, active, setActive })
  tabs: [id: string, label: string][] — array of tuples
  Current 4-tab usage in PlayAmericano lines 171–180:
    tabs={[ ["courts","⚔️ Pistas"], ["standings","🏆 Posiciones"], ["history","📜 Historial"], ["rules","📖 Reglas"] ]}

From src/components/play/PlayAmericano.jsx — critical lines:
  Line 114: const { courts: nc, sittingOut: nSit } = buildRoundAmericano(np, t.config.courts, nh, nso, t.config.mode);
    → THIS IS THE ONLY LINE CHANGING IN onNext()
  Line 144: const isFinished = !!(t.config.maxRounds && t.roundNum >= t.config.maxRounds);
    → EXTEND THIS
  Lines 171–180: <Tabs tabs={[...4 tabs...]} active={tab} setActive={setTab} />
    → ADD CONDITIONAL 5th ENTRY
  Line 201: {isAdmin && t.config.useLevels && (...)} — established isAdmin gate pattern
  Lines 472–480: yellow pill for sitting-out (bg-yellow-400/10 border-yellow-400/20) — reuse for Descansos rows
  Line 696: function FutureRound({...}) — sub-component pattern; WarningsBanner follows same file-scope placement

t.roundNum is 1-based (starts at 1 per initTournament.js).
  → t.precomputedRounds[0] = round 1
  → When on round N, next round is at t.precomputedRounds[N] (= index N, which is round N+1)
  → t.precomputedRounds[t.roundNum] is the NEXT round (correct)
  → t.precomputedRounds[t.roundNum - 1] is the CURRENT round (used elsewhere for display — do NOT use here)

t.roundWarnings is INSIDE the data JSON (inside t), NOT a Firestore top-level field.
  Access as: t.roundWarnings — same as t.players, t.rounds, etc.
  Gate: isAdmin && t.roundWarnings?.length > 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: onNext() wire-up and isFinished extension</name>
  <files>src/components/play/PlayAmericano.jsx</files>

  <read_first>
    - src/components/play/PlayAmericano.jsx — read lines 75–150 (onNext body + isFinished declaration). Understand the full onNext() scope before touching anything. Lines 76–113 (guard, score tally) and lines 121–135 (persist call) are UNCHANGED — only line 114 is replaced.
    - .planning/phases/02-playamericano-wire-up/02-PATTERNS.md — Change 1 and Change 2 sections contain the exact replacement patterns.
    - .planning/phases/01-core-penalty-engine/01-01-SUMMARY.md — Confirm precomputedRounds shape and roundWarnings persistence approach (roundWarnings is inside t, not a Firestore top-level field).
  </read_first>

  <action>
    Make exactly two edits to PlayAmericano.jsx. Do not touch any other lines.

    EDIT 1 — Replace line 114 (the buildRoundAmericano call) with the pre-calculated path + legacy fallback:

    Remove this single line (line 114):
      const { courts: nc, sittingOut: nSit } = buildRoundAmericano(np, t.config.courts, nh, nso, t.config.mode);

    Replace with this block (preserving all surrounding lines unchanged):
      let nc, nSit;
      if (t.precomputedRounds?.length && t.precomputedRounds[t.roundNum]) {
        const preRound = t.precomputedRounds[t.roundNum];
        nc = preRound.courts.map(c => ({ ...c, scoreA: "", scoreB: "", saved: false }));
        nSit = preRound.sittingOut;
      } else {
        ({ courts: nc, sittingOut: nSit } = buildRoundAmericano(
          np,
          t.config.courts,
          nh,
          nso,
          t.config.mode,
        ));
      }

    The index t.precomputedRounds[t.roundNum] is correct (not [t.roundNum - 1]):
    - t.roundNum is 1-based; when advancing from round N to N+1, roundNum=N, so precomputedRounds[N] = round N+1 (0-indexed). This is the next round.
    - The spread override ({ ...c, scoreA: "", scoreB: "", saved: false }) is mandatory even though Phase 1 already writes those values — it is a defensive guard against shape drift.

    EDIT 2 — Replace line 144 (isFinished definition) with the extended check:

    Remove (line 144):
      const isFinished = !!(t.config.maxRounds && t.roundNum >= t.config.maxRounds);

    Replace with:
      const isFinished = !!(
        (t.config.maxRounds && t.roundNum >= t.config.maxRounds)
        || (t.precomputedRounds?.length && t.roundNum > t.precomputedRounds.length)
      );

    The > operator (not >=) is correct: after onNext() completes the last round, roundNum is incremented to length+1. The > check fires at that state. >= would incorrectly fire while the last round is still being played (when roundNum === length).

    Do NOT change lines 76–113 (guard, isPairs, entityKey, np/nh/nso setup, score tally loop) or lines 121–135 (newRounds append, setLs({}), persist call). These are preserved exactly per D-08.
  </action>

  <verify>
    - grep -n "precomputedRounds\[t\.roundNum\]" "src/components/play/PlayAmericano.jsx" — must return at least 1 match (the onNext replacement)
    - grep -n "t\.roundNum > t\.precomputedRounds\.length" "src/components/play/PlayAmericano.jsx" — must return 1 match (isFinished extension)
    - grep -n "buildRoundAmericano(np" "src/components/play/PlayAmericano.jsx" — must still return 1 match (legacy fallback path preserved)
    - npm run build — must pass with no errors
  </verify>

  <acceptance_criteria>
    - PlayAmericano.jsx line ~114 contains the if/else block reading t.precomputedRounds[t.roundNum] — NOT the original buildRoundAmericano() call
    - PlayAmericano.jsx line ~144 contains both conditions joined by ||: the original maxRounds check AND the new t.roundNum > t.precomputedRounds.length check
    - buildRoundAmericano() still appears exactly once in the file (in the legacy else branch) — it is not removed
    - Lines before and after the replacement (score tally loop, persist call) are byte-for-byte identical to the original
    - The spread pattern is ({ ...c, scoreA: "", scoreB: "", saved: false }) — not a direct reference to preRound.courts[i]
    - npm run build passes with no TypeScript/lint errors
  </acceptance_criteria>

  <done>
    onNext() reads from t.precomputedRounds when populated, falls back to buildRoundAmericano() when not. isFinished correctly ends the tournament after the last pre-calculated round. Both pre-calculated and legacy paths tested to produce the same persist call shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Descansos tab and WarningsBanner sub-component</name>
  <files>src/components/play/PlayAmericano.jsx</files>

  <read_first>
    - src/components/play/PlayAmericano.jsx — read lines 165–360 (Tabs usage + tab content blocks) and lines 610–780 (HistoryRound, FutureRound sub-components at file scope). Understand the exact location of the {tab === "rules" && ...} block (the last existing tab block) and the sub-component definitions below the default export.
    - .planning/phases/02-playamericano-wire-up/02-PATTERNS.md — Change 3 (Tabs), Change 4 (Descansos block), Change 5 (WarningsBanner) contain exact markup to use.
    - .planning/phases/02-playamericano-wire-up/02-RESEARCH.md — Tabs component API section and Descansos Tab section for verified Tailwind classes.
  </read_first>

  <action>
    Make three edits to PlayAmericano.jsx. The first two are inside the default export; the third adds a file-scope sub-component after the existing FutureRound/HistoryRound definitions.

    EDIT 1 — Modify the Tabs array (lines 171–180) to add a conditional 5th tab:

    Find the existing Tabs JSX (starts with `<Tabs` and has the 4-tuple array). Add a conditional 5th entry via spread as the last element of the tabs array:

      ...(t.precomputedRounds?.length ? [["descansos", "💤 Descansos"]] : []),

    The full modified tabs prop becomes:
      tabs={[
        ["courts", "⚔️ Pistas"],
        ["standings", "🏆 Posiciones"],
        ["history", "📜 Historial"],
        ["rules", "📖 Reglas"],
        ...(t.precomputedRounds?.length ? [["descansos", "💤 Descansos"]] : []),
      ]}

    With 5 tabs on 390px, each button gets ~78px — the Tabs component uses flex:1 per button, this fits without overflow.

    EDIT 2 — Add the WarningsBanner usage site and the Descansos tab content block:

    2a. Inside the `<div style={{ padding: 16 }}>` wrapper (line ~170, just before the existing <Tabs> call), add the admin-gated WarningsBanner:

      {isAdmin && t.roundWarnings?.length > 0 && (
        <WarningsBanner warnings={t.roundWarnings} />
      )}

    Placement is ABOVE the <Tabs> call so the warning is always visible regardless of which tab is active. t.roundWarnings is accessed as a direct field on t (it is inside the data JSON, NOT a Firestore top-level field — same access pattern as t.players).
    NEVER use localStorage.getItem("isAdmin") — use the isAdmin prop that PlayAmericano already receives (per CLAUDE.md §9).

    2b. After the last existing tab block ({tab === "rules" && ...}), add the Descansos content block:

      {tab === "descansos" && t.precomputedRounds?.length > 0 && (
        <div className="max-w-lg mx-auto">
          <p className="text-xs text-gray-500 font-semibold mb-3">Jugadores que descansan por ronda</p>
          {t.precomputedRounds.map((round, i) => {
            const rNum = i + 1;
            const isCurrent = rNum === t.roundNum;
            const names = round.sittingOut?.length
              ? round.sittingOut.map(p => p.name).join(", ")
              : "Nadie descansa";
            return (
              <div
                key={i}
                className={`flex items-center gap-3 px-4 py-2.5 rounded-xl mb-1.5 text-sm ${
                  isCurrent
                    ? "bg-yellow-400/10 border border-yellow-400/20"
                    : "bg-[#1f2937] border border-gray-700"
                }`}
              >
                <span className={`font-black text-xs shrink-0 w-8 ${isCurrent ? "text-yellow-400" : "text-gray-500"}`}>
                  R{rNum}{isCurrent ? " ●" : ""}
                </span>
                <span className={isCurrent ? "text-yellow-200 font-medium" : "text-gray-400"}>
                  {names}
                </span>
              </div>
            );
          })}
        </div>
      )}

    Row display rules (per D-02, D-04):
    - Current round (rNum === t.roundNum): yellow bg-yellow-400/10 + border-yellow-400/20, text-yellow-400 label, text-yellow-200 names, "●" marker after round number
    - All other rounds: bg-[#1f2937] border-gray-700 (standard card), text-gray-500 label, text-gray-400 names
    - Empty sittingOut array: show "Nadie descansa" (per D-04 — every round has a row)
    - The Descansos tab is visible to everyone, not gated by isAdmin (per D-03)

    EDIT 3 — Add WarningsBanner as a file-scope sub-component after the last existing sub-component (FutureRound, which ends around line 780):

    function WarningsBanner({ warnings }) {
      const [open, setOpen] = useState(false);
      return (
        <div className="mb-2 rounded-xl border border-amber-500/30 bg-amber-500/10 overflow-hidden">
          <button
            onClick={() => setOpen(v => !v)}
            className="w-full flex items-center justify-between px-4 py-2.5 text-left"
          >
            <div className="flex items-center gap-2">
              <span className="text-amber-400 text-sm font-bold">⚠️ Restricciones relajadas</span>
              <span className="text-xs text-amber-500/80 bg-amber-500/20 px-2 py-0.5 rounded-full font-semibold">
                {warnings.length}
              </span>
            </div>
            <span className="text-amber-500/60 text-xs font-bold">{open ? "▲" : "▼"}</span>
          </button>
          {open && (
            <div className="px-4 pb-3 flex flex-col gap-1.5">
              {warnings.map((w, i) => (
                <div key={i} className="text-xs text-amber-200/80 bg-amber-500/5 rounded-lg px-3 py-2">
                  {w.message}
                </div>
              ))}
            </div>
          )}
        </div>
      );
    }

    The WarningsBanner uses its OWN useState(false) for collapse state. It is defined at FILE SCOPE below the default export — same pattern as CourtsAmericano (line 382), HistoryRound (line 614), FutureRound (line 696). Do NOT add warningsOpen state to the PlayAmericano function body — that would be inside the default export, not the sub-component.

    The w.message field is already in Spanish from Phase 1 (e.g., "Ronda 3: repetición de pareja permitida (sin combinación válida disponible)") — display it directly without translation.

    NO inline styles on any new elements — Tailwind classes only (per CLAUDE.md §7 rule 2). The amber color vocabulary (border-amber-500/30, bg-amber-500/10, text-amber-400) is distinct from the yellow sitting-out vocabulary — amber is intentional for warnings.
  </action>

  <verify>
    - grep -n '"descansos"' "src/components/play/PlayAmericano.jsx" — must return 2 matches: one in the Tabs array, one in the tab block
    - grep -n 'function WarningsBanner' "src/components/play/PlayAmericano.jsx" — must return 1 match at file scope (below the default export)
    - grep -n 'isAdmin && t\.roundWarnings' "src/components/play/PlayAmericano.jsx" — must return 1 match above the Tabs call
    - grep -n 'Nadie descansa' "src/components/play/PlayAmericano.jsx" — must return 1 match inside the Descansos tab block
    - npm run build — must pass with no errors
  </verify>

  <acceptance_criteria>
    - The Tabs array contains ...(t.precomputedRounds?.length ? [["descansos", "💤 Descansos"]] : []) as the 5th entry — legacy tournaments (no precomputedRounds) never show the Descansos tab
    - {tab === "descansos" && t.precomputedRounds?.length > 0 && (...)} block exists after the {tab === "rules" && ...} block
    - Descansos block iterates t.precomputedRounds.map((round, i) => ...) with rNum = i + 1 and isCurrent = rNum === t.roundNum
    - Current round row uses className containing "bg-yellow-400/10" and "border border-yellow-400/20"
    - Non-current rows use className containing "bg-[#1f2937]" and "border border-gray-700"
    - Empty sittingOut (length 0 or undefined) renders "Nadie descansa" — not an empty string, not a missing row
    - {isAdmin && t.roundWarnings?.length > 0 && <WarningsBanner warnings={t.roundWarnings} />} appears inside the padding wrapper, ABOVE the <Tabs> call
    - function WarningsBanner({ warnings }) is defined at file scope, after the default export, with its own const [open, setOpen] = useState(false)
    - WarningsBanner renders warnings.map((w, i) => ...) showing w.message in each row
    - No inline styles on any new elements — all Tailwind classes
    - npm run build passes with no errors
  </acceptance_criteria>

  <done>
    Descansos 5th tab appears for pre-calculated tournaments showing every round's rest assignment. WarningsBanner appears above tabs for admin only when t.roundWarnings is non-empty. Both are invisible for legacy tournaments (no precomputedRounds, no roundWarnings).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → PlayAmericano | t.roundWarnings and t.precomputedRounds are read from Firestore via onSnapshot; treated as trusted data (organizer wrote them at tournament start) |
| isAdmin gate | Computed in useTournament.js as auth.currentUser.uid === t.ownerUid — pure Firebase Auth, no client-side bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | isAdmin check for WarningsBanner | mitigate | Gate is `isAdmin && t.roundWarnings?.length > 0` where isAdmin comes from Firebase Auth comparison in useTournament — never from localStorage or URL params. CLAUDE.md §9 enforces this. |
| T-02-02 | Information Disclosure | WarningsBanner content visible to spectators | mitigate | isAdmin prop gates the entire WarningsBanner render. Spectators receive t via onSnapshot but the JSX never renders the warnings DOM for non-admins. |
| T-02-03 | Tampering | Client modifying t.precomputedRounds or t.roundWarnings in-browser | accept | These fields are display-only in Phase 2 — no writes to either field occur here. Modifying them client-side only affects the local view. Firestore Security Rules (pending hardening per CLAUDE.md) will enforce write ownership. Low risk for passive display. |
| T-02-04 | Denial of Service | Very large t.roundWarnings array rendering slowly on mobile | accept | warnings[] count is bounded by totalRounds (typically 7–12). Rendering 12 amber div rows is trivially fast. No pagination needed at this scale. |
| T-02-05 | Tampering | npm/pip/cargo installs | accept | Phase 2 introduces no new packages — Package Legitimacy Audit not applicable. |
</threat_model>

<verification>
Manual end-to-end verification after execution (checkpoint replaced by inline checks):

1. New pre-calculated tournament (useLevels=ON, 8+ players):
   - Start tournament → "Siguiente Ronda" button advances through each round consuming t.precomputedRounds entries
   - After the last pre-calculated round, "Siguiente Ronda" is gone (isFinished fires)
   - Descansos tab appears as 5th tab; shows all rounds; current round is highlighted yellow
   - Admin sees WarningsBanner above tabs when any constraint was relaxed; non-admin sees nothing

2. Legacy tournament (no precomputedRounds):
   - No Descansos tab appears
   - onNext() still calls buildRoundAmericano() without errors
   - No WarningsBanner appears (t.roundWarnings is undefined)

3. Mobile layout at 390px:
   - 5 tabs render without overflow (each tab ~78px)
   - Descansos rows are readable with max-w-lg mx-auto wrapper

Automated:
   - npm run build — zero errors
   - npm test (vitest) — all 75+ tests pass (no Phase 2 changes to algorithm or tests)
</verification>

<success_criteria>
All four ROADMAP Phase 2 success criteria are TRUE:

1. When t.precomputedRounds is populated, onNext() reads t.precomputedRounds[t.roundNum] instead of calling buildRoundAmericano()
   Verified by: grep for precomputedRounds[t.roundNum] in the if branch; manual round advancement in pre-calculated tournament

2. When t.precomputedRounds is null or empty (legacy tournament), onNext() falls back to buildRoundAmericano() without errors
   Verified by: legacy else branch preserved; legacy tournament advancement works without console errors

3. An admin-only warnings panel appears when t.roundWarnings[] is non-empty, listing which rounds had relaxed constraints and why
   Verified by: isAdmin && t.roundWarnings?.length > 0 gate; WarningsBanner renders w.message per entry; spectator sees nothing

4. A rest schedule panel shows all rounds at a glance with sitting-out player(s) per round, readable on a 390px mobile screen
   Verified by: Descansos tab present when precomputedRounds populated; every round row present; max-w-lg mx-auto; "Nadie descansa" for empty slots
</success_criteria>

<output>
Create `.planning/phases/02-playamericano-wire-up/02-01-SUMMARY.md` when done.
</output>
