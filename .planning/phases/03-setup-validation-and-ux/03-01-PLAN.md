---
phase: 03-setup-validation-and-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/setup/SetupAmericano.jsx
autonomous: false
requirements: [SETUP-01, SETUP-02]
tags: [setup, americano, validation, ux, reshuffle]

must_haves:
  truths:
    - "Con useLevels=ON y todos los jugadores en nivel 0, aparece un banner amarillo de advertencia antes del botón Iniciar"
    - "El organizador puede descartar el banner (Continuar) y el botón Iniciar sigue funcionando — el warning NO bloquea el inicio"
    - "Existe un botón de re-sorteo visible durante el setup que llama precomputeAllRounds() y guarda el resultado solo en estado local"
    - "handleReshuffle NO llama persist() — el schedule local solo se persiste al pulsar Iniciar Torneo"
    - "El botón de re-sorteo está ausente cuando t.status === playing"
    - "onStart() usa localPrecomputedRounds si existe; si no, recalcula como antes"
  artifacts:
    - path: "src/components/setup/SetupAmericano.jsx"
      provides: "Banner de niveles sin asignar (SETUP-01) + botón re-sorteo con estado local (SETUP-02)"
      contains: "handleReshuffle"
  key_links:
    - from: "src/components/setup/SetupAmericano.jsx handleReshuffle()"
      to: "precomputeAllRounds()"
      via: "llamada directa con entities + t.config"
      pattern: "precomputeAllRounds\\(entities, t\\.config\\)"
    - from: "src/components/setup/SetupAmericano.jsx onStart()"
      to: "localPrecomputedRounds"
      via: "rama condicional que prefiere el estado local"
      pattern: "localPrecomputedRounds !== null"
---

<objective>
SetupAmericano guía al organizador a través de la asignación de niveles antes de iniciar el torneo.

## Phase Goal

**As a** organizador de un Americano Clásico, **I want to** ser advertido cuando ningún jugador tiene nivel asignado y poder re-sortear el emparejamiento antes de iniciar, **so that** confirmo conscientemente el schedule que se persiste y no inicio un torneo donde la feature de niveles no tiene efecto sin saberlo.

Purpose: Cierra los dos últimos requisitos de UI de setup (SETUP-01, SETUP-02) sobre el motor de pre-cálculo ya construido en Phase 1. Es una capa de UX puramente cliente — sin cambios en la capa de lógica ni en Firebase.

Output: `src/components/setup/SetupAmericano.jsx` con un banner de advertencia suave descartable y un botón de re-sorteo con estado local, ambos gated por `isAdmin`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/03-setup-validation-and-ux/03-RESEARCH.md
@.planning/phases/03-setup-validation-and-ux/03-PATTERNS.md
@.planning/phases/01-core-penalty-engine/01-01-SUMMARY.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del codebase real. No se requiere exploración adicional. -->

Firma de precomputeAllRounds (src/logic/americano.js:103-108, ya importada en línea 3 de SetupAmericano.jsx):
```js
precomputeAllRounds(entities, config)
// entities: Array<{ id: number, name: string, level: number, pts: 0, gf: 0, gc: 0 }>
// config: t.config { courts, mode, matchmaking?, maxRounds?, ... }
// Returns: { rounds: Array<{courts, sittingOut}> | null, warnings: Array<{round, constraint, message}> }
// rounds es null cuando mode === "pairs"
```

Valores de nivel (src/components/setup/SetupAmericano.jsx:14-19):
```
nivel 0 = "Sin definir"  (Unrated)
nivel 3 = "Avanzado"     (umbral de highLevelClash)
// El código usa (p.level || 0) como fallback en toda la lógica.
```

Constantes derivadas existentes (líneas 80-85), reutilizar tal cual:
```js
const ok = isPairs ? (...) : tot >= 4 && (t.playerInputs || []).every(p => p.name.trim().length > 0);
const useLevels = !!t.config.useLevels;
const isPairs = t.config.mode === "pairs"; // línea 33
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar estado local, condiciones derivadas y handleReshuffle (SETUP-02 lógica + SETUP-01 condición)</name>
  <files>src/components/setup/SetupAmericano.jsx</files>
  <read_first>
    - src/components/setup/SetupAmericano.jsx (líneas 1-200: bloque useState 35-54, constantes derivadas 69-85, handlers debounce 87-138, onStart 156-185)
    - .planning/phases/03-setup-validation-and-ux/03-PATTERNS.md (Pattern 1, 2, 3, 4)
    - .planning/phases/01-core-penalty-engine/01-01-SUMMARY.md (Final Return Shape — confirma { rounds, warnings })
  </read_first>
  <action>
    Tres ediciones quirúrgicas en SetupAmericano.jsx, todas siguiendo las convenciones de alineación existentes del archivo.

    (1) NUEVO ESTADO — Insertar inmediatamente después de la línea 54 (tras `const [editP2, setEditP2] = useState("");`), siguiendo el mismo formato de alineación con columnas:
        const [warningDismissed,       setWarningDismissed]       = useState(false);
        const [localPrecomputedRounds, setLocalPrecomputedRounds] = useState(null);
        const [localRoundWarnings,     setLocalRoundWarnings]     = useState([]);
    `localRoundWarnings` se incluye porque `onStart()` (Task 2) lo usará para persistir los warnings del schedule aceptado, en paridad con `roundWarnings` actual.

    (2) CONDICIONES DERIVADAS — Insertar inmediatamente después de la línea 85 (tras `const useLevels = !!t.config.useLevels;`):
        const allUnrated = useLevels && !isPairs &&
          (t.playerInputs || []).length > 0 &&
          (t.playerInputs || []).every(p => (p.level || 0) === 0);
        const showUnratedWarning = allUnrated && !warningDismissed;

        const canReshuffle = !isPairs &&
          (t.config.matchmaking || "americano") === "americano" &&
          t.status !== "playing" &&
          ok;
    CRÍTICO — La guarda `(t.playerInputs || []).length > 0` es OBLIGATORIA: sin ella `.every()` devuelve `true` por vacuous-truth sobre array vacío y el banner aparece antes de agregar jugadores (Pitfall 1 del research). NO eliminarla.
    `canReshuffle` usa `ok` (definido en línea 80) como umbral — el botón dispara también el primer cálculo, por lo que NO se condiciona a `localPrecomputedRounds !== null` (Pattern 2 / Open Question 1 del research). El check `t.config.matchmaking === "americano"` evita que el botón aparezca en modo mexicano (Pitfall 4).

    (3) HANDLER — Insertar `handleReshuffle` junto a los demás handlers (después de `handleMinutesCustom`, que termina en línea 138, y antes de `addPlayer` en línea 140). Función SÍNCRONA (NO async, NO try/catch — no toca Firebase):
        function handleReshuffle() {
          if (isPairs) return;
          const entities = (t.playerInputs || []).map((p, i) => ({
            id: i, name: p.name.trim(), level: p.level || 0, pts: 0, gf: 0, gc: 0
          }));
          const result = precomputeAllRounds(entities, t.config);
          setLocalPrecomputedRounds(result.rounds);
          setLocalRoundWarnings(result.warnings);
        }
    PROHIBIDO: que `handleReshuffle` llame a `persist()`. El punto central de SETUP-02 es que el re-sorteo es solo local hasta que el organizador pulse "Iniciar Torneo" (REQUIREMENTS.md SETUP-02 + Pattern 3). NO agregar persist, NO agregar debounce.
  </action>
  <verify>
    <automated>cd "/Users/ximeyfede/Desktop/Padel app" && grep -v '^[[:space:]]*//' src/components/setup/SetupAmericano.jsx | grep -c "handleReshuffle\|localPrecomputedRounds\|warningDismissed\|allUnrated\|canReshuffle" | awk '$1 >= 8 {exit 0} {exit 1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: existe `function handleReshuffle()` síncrona en SetupAmericano.jsx
    - Source assertion: `handleReshuffle` contiene `precomputeAllRounds(entities, t.config)` y NO contiene `persist(` ni `setTimeout`
    - Source assertion: `allUnrated` incluye literalmente `(t.playerInputs || []).length > 0`
    - Source assertion: `canReshuffle` incluye `t.status !== "playing"` y `(t.config.matchmaking || "americano") === "americano"`
    - Behavior assertion: con `t.playerInputs = []`, `allUnrated` evalúa a `false` (la guarda de longitud previene vacuous-truth)
    - `npm run build` pasa sin errores
  </acceptance_criteria>
  <done>El estado local, las condiciones derivadas y `handleReshuffle()` existen y compilan. handleReshuffle no persiste a Firebase.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar banner (SETUP-01) y botón re-sorteo (SETUP-02) en JSX + integrar onStart()</name>
  <files>src/components/setup/SetupAmericano.jsx</files>
  <read_first>
    - src/components/setup/SetupAmericano.jsx (líneas 156-185 onStart; líneas 779-794 bloque botón Iniciar)
    - .planning/phases/03-setup-validation-and-ux/03-PATTERNS.md (Pattern 4 onStart, Pattern 5 banner, Pattern 6 botón re-sorteo)
    - src/components/play/PlayAmericano.jsx (líneas 853-879: WarningsBanner — referencia visual de estilo amber/yellow)
  </read_first>
  <action>
    Dos inserciones JSX y una modificación quirúrgica de onStart(). El bloque `{isAdmin && (<button onClick={onStart} ...>)}` de las líneas 779-794 NO se modifica — las nuevas piezas se insertan ANTES de él.

    (1) BANNER SETUP-01 — Insertar inmediatamente antes del comentario `{/* ── Botón iniciar ── */}` (línea 779). Banner amarillo (token `yellow`, distinto del `amber` de WarningsBanner para diferenciar "sin nivel" de "restricciones relajadas"). Solo Tailwind, sin estilos inline (CLAUDE.md sección 7). Gated por `isAdmin`:
        {isAdmin && showUnratedWarning && (
          <div className="mt-4 rounded-xl border border-yellow-500/30 bg-yellow-500/10 px-4 py-3">
            <div className="flex items-start justify-between gap-3">
              <div>
                <div className="text-sm font-bold text-yellow-400">⚠️ Ningún jugador tiene nivel asignado</div>
                <div className="text-xs text-yellow-300/70 mt-1">
                  El emparejamiento por niveles no tendrá efecto. Puedes asignar niveles arriba o continuar sin ellos.
                </div>
              </div>
              <button
                onClick={() => setWarningDismissed(true)}
                className="text-yellow-500/60 hover:text-yellow-400 text-xs font-bold shrink-0 cursor-pointer transition-colors"
              >
                Continuar
              </button>
            </div>
          </div>
        )}
    CRÍTICO: SETUP-01 es un soft warning. NO modificar la constante `ok` ni la prop `disabled={!ok}` del botón Iniciar. El botón Iniciar debe seguir clickeable aunque el banner esté visible (research Anti-Pattern: "No colocar el banner como bloqueo hard").

    (2) BOTÓN RE-SORTEO SETUP-02 — Insertar inmediatamente DESPUÉS del banner SETUP-01 y ANTES del comentario `{/* ── Botón iniciar ── */}`. Estilo secundario estático (solo Tailwind). Texto dinámico según si ya hay schedule local:
        {isAdmin && canReshuffle && (
          <button
            onClick={handleReshuffle}
            className="w-full mt-3 py-2.5 rounded-xl font-bold text-sm border border-gray-700 bg-[#1f2937] text-gray-300 hover:text-gray-100 hover:border-gray-500 transition-colors cursor-pointer"
          >
            {localPrecomputedRounds !== null ? "✓ Emparejamiento listo — Re-sortear" : "🔀 Generar emparejamiento"}
          </button>
        )}
    El texto ternario es la única confirmación visual del schedule calculado en v1 (research Open Question 2 — NO construir panel de preview en esta fase).

    (3) INTEGRAR onStart() — Reemplazo quirúrgico de las líneas 166-170. El bloque actual es:
        if ((t.config.matchmaking || "americano") === "americano") {
          const result = precomputeAllRounds(entities, t.config);
          precomputedRounds = result.rounds;
          roundWarnings = result.warnings;
        }
    Reemplazar SOLO el interior del `if` por la rama que prefiere el estado local:
        if ((t.config.matchmaking || "americano") === "americano") {
          if (localPrecomputedRounds !== null) {
            precomputedRounds = localPrecomputedRounds;
            roundWarnings = localRoundWarnings;
          } else {
            const result = precomputeAllRounds(entities, t.config);
            precomputedRounds = result.rounds;
            roundWarnings = result.warnings;
          }
        }
    El `await persist({ ...t, ..., precomputedRounds, roundWarnings })` de las líneas 171-183 NO cambia — sigue usando `{ merge: true }` vía useTournament (CLAUDE.md regla de oro del persist). NO mover `precomputedRounds`/`roundWarnings` a top-level de Firestore — siguen dentro del JSON de `data`.
  </action>
  <verify>
    <automated>cd "/Users/ximeyfede/Desktop/Padel app" && npm run build 2>&1 | tail -3 && grep -q "showUnratedWarning" src/components/setup/SetupAmericano.jsx && grep -q "canReshuffle" src/components/setup/SetupAmericano.jsx && grep -q "localPrecomputedRounds !== null" src/components/setup/SetupAmericano.jsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: el JSX del banner contiene `setWarningDismissed(true)` y está gated por `isAdmin && showUnratedWarning`
    - Source assertion: el botón re-sorteo está gated por `isAdmin && canReshuffle` y su onClick es `handleReshuffle`
    - Source assertion: onStart contiene la rama `if (localPrecomputedRounds !== null)` dentro del bloque `matchmaking === "americano"`
    - Source assertion: la constante `ok` y `disabled={!ok}` NO fueron modificadas (el warning no bloquea)
    - Source assertion: el banner y el botón re-sorteo usan SOLO clases Tailwind (sin `style={{...}}` nuevo)
    - Behavior assertion: con `t.status === "playing"`, `canReshuffle` es `false` → el botón no se renderiza
    - `npm run build` pasa sin errores ni warnings sobre el archivo modificado
  </acceptance_criteria>
  <done>Banner SETUP-01 y botón SETUP-02 renderizan gated por isAdmin; onStart prefiere localPrecomputedRounds; build limpio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Banner de advertencia de niveles sin asignar (SETUP-01) y botón de re-sorteo de emparejamiento (SETUP-02) en la pantalla de setup del Americano. No existe infraestructura de tests de componentes en el proyecto (solo vitest para lógica), por lo que la verificación visual/funcional la confirma el organizador.
  </what-built>
  <how-to-verify>
    1. Ejecutar `npm run dev` y abrir la app en un navegador a 390px de ancho (vista mobile).
    2. Crear un torneo Americano, activar "usar niveles" (useLevels=ON) en config, y agregar 4+ jugadores SIN asignarles nivel (dejar todos en "Sin definir").
    3. SETUP-01 — Verificar que aparece el banner amarillo "⚠️ Ningún jugador tiene nivel asignado" justo encima del botón "Iniciar Torneo". Pulsar "Continuar" → el banner desaparece y el botón "Iniciar Torneo" sigue habilitado y clickeable.
    4. Asignar un nivel a algún jugador → confirmar que el banner desaparece solo (sin necesidad de descartar).
    5. SETUP-02 — Con jugadores suficientes (ok=true) y matchmaking=americano, verificar que aparece el botón "🔀 Generar emparejamiento". Pulsarlo → el texto cambia a "✓ Emparejamiento listo — Re-sortear". Pulsarlo de nuevo varias veces (no debe persistir nada todavía).
    6. Pulsar "Iniciar Torneo" → el torneo arranca con el último schedule visto. Verificar que la lista de rondas/descansos en PlayAmericano coincide con el re-sorteo aceptado.
    7. Confirmar que tras iniciar (status=playing) el botón de re-sorteo ya NO aparece.
    8. Cambiar matchmaking a "mexicano" en setup → confirmar que el botón de re-sorteo NO aparece.
  </how-to-verify>
  <resume-signal>Escribe "approved" si todo funciona, o describe los problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizador (cliente) → estado local React | Input del organizador (niveles, clics de re-sorteo) modifica solo estado en memoria; nada cruza a Firebase hasta "Iniciar Torneo" |
| SetupAmericano → Firestore (vía useTournament.persist) | Solo `onStart()` cruza esta frontera; usa `{ merge: true }` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Banner SETUP-01 + botón re-sorteo | mitigate | Todo el JSX nuevo está gated con `{isAdmin && ...}`; `isAdmin` se deriva de `auth.currentUser.uid === t.ownerUid` (no localStorage) — un espectador no ve ni puede disparar re-sorteo |
| T-03-02 | Tampering | handleReshuffle → estado del torneo | mitigate | `handleReshuffle` NO llama `persist()` — no puede mutar el documento Firestore; el resultado vive solo en estado local hasta confirmación explícita del organizador en `onStart()` (que usa `{ merge: true }`, sin sobreescribir ownerUid/createdAt) |
| T-03-03 | Denial of Service | botón re-sorteo (clics repetidos) | accept | `precomputeAllRounds` corre <50ms para 24 jugadores (verificado Phase 1); clics rápidos solo recalculan en memoria, sin coste de red ni Firestore |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No se instalan paquetes nuevos en esta fase (RESEARCH.md: "Package Legitimacy Audit — no aplicable"); sin superficie de cadena de suministro |
</threat_model>

<verification>
1. `npm run build` pasa sin errores ni warnings sobre `SetupAmericano.jsx`.
2. `npm test` (vitest) sigue verde — Phase 3 no toca la capa de lógica, los 75 tests existentes deben seguir pasando.
3. Source: `grep -c "isAdmin && showUnratedWarning\|isAdmin && canReshuffle" src/components/setup/SetupAmericano.jsx` ≥ 2 (ambos bloques gated por isAdmin).
4. Source: `handleReshuffle` no contiene `persist(`.
5. Checkpoint humano confirma comportamiento visual/funcional en 390px.
</verification>

<success_criteria>
1. Con `useLevels=ON` y todos los jugadores en nivel 0, aparece un banner amarillo descartable antes del botón Iniciar; al pulsar "Continuar" el banner desaparece y el botón Iniciar permanece habilitado (soft warning, no bloqueo). [SETUP-01]
2. Un botón de re-sorteo visible durante el setup re-ejecuta `precomputeAllRounds()` y reemplaza el schedule en estado local SIN persistir a Firestore; `onStart()` persiste el schedule local aceptado. [SETUP-02]
3. El botón de re-sorteo está ausente cuando `t.status === "playing"` y cuando `matchmaking !== "americano"`. [SETUP-02]
4. `npm run build` limpio; los 75 tests de lógica siguen verdes; sin estilos inline nuevos; sin `console.log`.
</success_criteria>

<output>
Crear `.planning/phases/03-setup-validation-and-ux/03-01-SUMMARY.md` al terminar.
</output>
